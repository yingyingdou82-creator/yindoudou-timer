# 银豆豆计时 — 项目说明书（CLAUDE.md）

> 本文件是 Claude Code 的项目说明书，每次对话都会自动加载。
> 所有参与本项目的 AI 助手必须严格遵守本文件的规则，尤其是【第一部分：项目协作规则】。

---

## 第一部分：项目协作规则（最高优先级，必须遵守）

1. **用户背景**：用户是不懂编程技术的非技术用户，无法提供具体的技术要求。

2. **一切技术决策必须先列方案，由用户挑选决定**：
   - 任何技术选型（框架、数据库、第三方库、工具、重大结构改动等），必须列出**至少 2 个候选方案**；
   - 用通俗易懂的中文解释每个方案：它是什么、优点、缺点；
   - 给出推荐方案及推荐理由；
   - **由用户挑选决定**。未经用户选择，不得擅自敲定任何技术方案。

3. **每个技术点都要详细解释**：向用户介绍或使用任何技术时，必须用大白话解释清楚：这个技术是什么、为什么用它、对用户有什么影响；避免专业术语，不得不用的术语必须先用一两句话解释明白。

4. **沟通语言**：全程使用中文交流。

5. **重大改动先征得同意**：涉及 Git 推送、安装软件、修改系统设置、对外发送内容等操作，必须先讲清方案、征得用户同意后再执行。

---

## 第二部分：产品需求文档（v1 版本）

### 2.1 产品概述

- **产品名称**：银豆豆计时
- **产品定位**：一款简洁可爱的桌面日期倒计时 / 正计时工具，帮用户记住生活中所有重要的日子（考试、生日、纪念日、项目截止日、还贷日、假期……）
- **运行平台**：Windows 10/11 电脑、macOS 12 及以上的 Mac 电脑
- **数据隐私**：所有数据只保存在用户自己的电脑上，不上传云端
- **界面语言**：全中文界面

### 2.2 核心功能清单

#### 功能一：事件管理

- 创建、编辑、删除倒计时事件
- 支持置顶（重要的事件排在最前面）
- 事件包含的信息（字段）：
  | 字段 | 说明 | 例子 |
  |---|---|---|
  | 名称 | 这个日子的名字 | 距离春节 / 宝宝出生 |
  | 日期 | 填一个日期即可：过去的日期自动按正计时，未来的自动按倒计时 | 2027-02-17 |
  | 时间（可选） | 几点几分，比赛、报名等准时开始的事情用；当天会按「小时/分钟」精确显示 | 14:30 |
  | 截止日期（可选） | 和开始日期组成「时间段」（比赛、报名期）；填了以后显示自动分三阶段切换 | 9月20日～10月15日 |
  | 提前提醒（可选） | 到点前用系统通知提醒，提前时长可选；需填了时间才可用 | 提前 30 分钟 |
  | 计数方式 | 自然日 / 工作日 / 按周 / 按年 | 见功能三 |
  | 包含起始日 | 起算的第一天算不算进计时 | 打开后 9 月 1 日开始记，当天就算第 1 天 |
  | 颜色标签 | 给事件配一个颜色，方便区分 | 红色 / 蓝色 |
  | 小图标 | 给事件配一个小图案 | 🎂 📚 ✈️ |
  | 背景图片（可选） | 给事件配一张图片，作为卡片背景显示；四种方式可选图：按钮选择 / 拖拽文件进来 / Ctrl+V 粘贴截图 / 从网页直接拖图片（自动压缩，文字自动加遮罩保证可读） | 比赛海报、宝宝照片 |
  | 备注 | 补充说明文字（可选） | 记得买蛋糕 |
  | 是否置顶 | 排在列表最上面 | 是 / 否 |
  | 归档 | 完成的事件收进"归档"，主界面不再显示，可随时找回/恢复/删除 | 是 / 否 |
  | 显示在桌面小组件 | 是否把这个事件放到桌面的悬浮面板上 | 是 / 否 |

#### 功能二：计时方向自动判断（无需手动选择）

- 添加事件时只需要填一个日期，软件自动判断方向：
  - 日期在**未来**：按**倒计时**显示——例：「距离考研还有 88 天」
  - 日期在**过去**：按**正计时（正向记录）**显示——例：「宝宝出生已经 100 天」「戒烟第 36 天」
  - 日期是**今天**：「就是今天！」
- 倒计时到期之后，从第二天起自动转为正计时显示（如「已经 2 天」），无需手动修改
- 填了**具体时间**的事件，当天自动切换成精确显示：如「还有 2 小时 15 分钟」；时间一过变成「已经 40 分钟」
- **时间段事件**（填了截止日期，如比赛/报名期）自动分三阶段显示，无需手动切换：
  - 还没到开始日期：「距离开始还有 X 天」
  - 进行中（开始日～截止日前一天）：自动变成「距离截止还有 X 天」；截止当天显示「今天截止！」
  - 已结束：「已结束 X 天」
  - 时间段事件不提供「包含起始日」开关；提前提醒仍按**开始时间**计算

#### 功能三：四种计数方式（每个事件单独选择用哪一种）

- **自然日**：每过一天算一天（默认，最常用），如「还有 88 天」「已经 100 天」
- **工作日**：只数工作日，周末不算

  - 默认规则：周一至周五算工作日，周六、周日不算
  - 可选开关：「叠加法定节假日」——打开后，软件按内置的国家放假安排计算：春节等法定假日不算工作日，调休补班的周末算工作日（例如：2026 年 9 月 20 日周日补班，计为工作日）
  - 节假日数据已内置 **2025、2026 两年**（来源：国务院办公厅通知，代码在 src/shared/holidays.ts）；国家每年 11 月左右公布次年安排，公布后补一段新数据即可；没有数据的年份自动按周一至周五计算（2027 年待公布）
- **按周显示**：用「几周几天」的组合形式显示
  - 倒计时：「还有 1 周 2 天」；正计时：「已经 1 周 2 天」
  - 恰好整周时只显示周数：「还有 3 周」；不足一周时直接显示天数：「还有 4 天」
- **按年显示**：用「几年几个月几周几天」从大到小组合显示
  - 倒计时：「还有 1 年 1 个月 3 周 4 天」；正计时：「已经 1 年 2 个月 5 天」
  - **为 0 的单位不显示**：正好一年就只显示「还有 1 年」，不会出现「1 年 0 个月 0 周 0 天」；中间为 0 的单位同样省略（如 1 年零 3 周，显示「还有 1 年 3 周」）
  - 计算按日历进行：年按对日算（1 月 1 日起算，次年 1 月 1 日满 1 年），月按日历月算，周每满 7 天算一周，最后剩下不足一周的按天显示

**通用选项「是否包含起始日」**（每个事件单独设置，默认不包含）：

- 打开后，起算的第一天也算进计时，整体多算 1 天：
  - 正计时：起始当天算第 1 天。例：9 月 1 日开始记，今天 9 月 16 日——不包含显示「已经 15 天」，包含显示「已经 16 天」
  - 倒计时：今天算第 1 天。例：目标 9 月 20 日，今天 9 月 16 日——不包含显示「还有 4 天」，包含显示「还有 5 天」
- 对自然日、工作日、按周、按年四种方式都生效（多算的 1 天会自动换算进周 / 月 / 年里）

#### 功能四：桌面小组件（悬浮面板）⭐ 核心特色

- 一块竖长的小面板（「豆荚」卡片）常驻桌面，始终显示在所有窗口最上层，随时能看到所有重要日子的倒计时
- **自由选择展示内容**：在主窗口里，每个事件都有「显示到桌面小组件」的开关——勾选哪些事件，桌面上就显示哪些；小组件里每条右侧也有小按钮，可快速移除某条
- **高度自动伸缩**：面板高度随展示的事件条数自动变化，每多勾选一条就自动长高一格，取消勾选就自动缩短，无需手动调节
  - 具体尺寸由 AI 设计，开发时可微调：每条事件占一行，行高约 48～56 像素（像素即屏幕上小圆点的数量，数字越大显示越大）；面板宽度三档可选——小（约 220）、中（约 300，默认）、大（约 380），字号随档位一起变化
  - 上限保护：事件特别多时，面板最高不超过屏幕高度的 80%，超出部分在面板内部上下滑动查看
- 可以用鼠标拖动到屏幕任意位置，位置自动记住（下次打开还在原地）
- 透明度可调；深色 / 浅色跟随主界面主题
- 点击面板上的某条事件，会打开主窗口并定位到该事件，方便编辑
- **技术说明**：「小组件」采用悬浮窗方案（不依赖 Windows / macOS 系统自带的小组件面板），两个平台体验完全一致

**实现说明（2026-09-16 已实现）**：
- 主窗口右上角「🖥 小组件」按钮：点击弹出**选择清单**——顶部开关控制面板显示/隐藏，下方勾选任意事件上桌面，改动即时生效
- 面板顶部标题栏按住可拖动，位置自动记住；右上角三个小按钮：宽度档位（窄/中/宽）、透明度、隐藏
- 每条事件一行（按事件颜色配色），悬停出现 ✕ 可快速移下桌面；点击条目呼出主窗口
- 事件的增删改在主窗口和小组件之间自动同步
- 关闭小组件**不会**退出软件；主窗口若被藏起会自动弹回。真正退出 = 主窗口关闭且小组件未显示

#### 功能五：主窗口

- 事件卡片列表：每张卡片显示事件名、天数、颜色、图标
- 排序方式：按剩余天数排序（最近的在最上）/ 自定义拖动排序
- 搜索框：按名称快速找到事件
- 深色 / 浅色主题切换
- 关闭窗口时最小化到系统托盘（屏幕右下角小图标），不占任务栏，软件在后台保持运行
- 可选设置：开机自动启动

**实现说明（2026-09-16 已实现）**：
- **去掉了系统默认标题栏，界面自绘标题栏**：顶部一整条都是银豆豆风格，按住可拖动窗口，双击空白处最大化/还原；右侧 – □ ✕ 三个小按钮（✕ 是隐藏到托盘）。Mac 上保留系统红绿灯按钮并自动留位
- 「⚙ 设置」弹出面板：深色模式开关（主窗口和小组件即时同步切换）、**字体大小（小/标准/大/特大四档，用 html zoom 实现，电脑/手机/小组件同步）**、开机自启开关（**只在正式安装包里生效**，开发模式下无效）
- 系统托盘（右下角银豆豆图标）：左键点一下 = 打开主窗口；右键菜单 = 打开主窗口 / 显示桌面小组件（勾选）/ 退出
- **退出软件的唯一方式是托盘菜单的「退出」**——关窗口只是隐藏到后台，提醒功能继续工作
- 排序目前为"置顶 → 离今天最近"，自定义拖动排序在后续版本补充
- 托盘图标由 scripts/gen-icon.js 纯代码生成（银色豆子），改图标改这个脚本再运行即可

#### 功能六：数据安全与备份

- 数据保存在本地 JSON 文件中（位置：系统标准的用户数据目录）
- 支持导出备份文件（一个 `.json` 小文件）
- 支持从备份文件导入（换电脑、重装系统后可恢复全部数据）

**实现说明（2026-09-16 已实现）**：
- 入口在「⚙ 设置」弹窗里：导出 / 导入 两个按钮
- 导出：全部事件 + 全部背景图片打包成**一个**备份文件（图片转成文本内嵌），文件名自动带日期
- 导入时会让您选：**合并导入**（加到现有事件后面，推荐换电脑用）或**替换导入**（清空现有，完全变成备份内容）
- 备份文件校验：不是银豆豆的备份文件会拒绝；单条坏数据自动跳过，不影响其他

#### 功能八：归档（2026-09-16 用户要求新增）

- 卡片上可直接「归档」：完成的事情（比完的赛、过完的纪念日）收起来，主界面保持清爽
- 归档后：不在主列表、不占搜索结果、不上桌面小组件、不再弹提醒
- 主界面底部有「📦 归档的事件（N）」入口，点开抽屉可查看全部归档
- 归档的事件可「恢复」回主界面，也可直接删除；数据完整保留

#### 功能七：到点提醒

- 给设置了时间的事件可开启「提前提醒」，提前时长可选：5 / 10 / 15 / 30 分钟、1 / 12 小时、1 天
- 提醒以系统通知形式弹出（Windows 右下角通知、Mac 右上角通知），点击通知可回到软件窗口
- 注意：提醒需要软件当时处于运行状态（含最小化到托盘）；软件没开时错过的提醒不补发

### 2.3 界面设计方向

- **「银豆豆」视觉风格**：圆润的豆子造型卡片、银白色主色调、柔和的低饱和度配色，可爱但清爽，不花哨
- 配了背景图片的事件卡片以图片为背景，上面盖一层半透明暗色渐变，文字切换为白色，保证任何图片上都看得清
- 主窗口左侧为事件列表、右侧或顶部为操作区（具体布局开发时以界面草图和用户确认为准）
- 深浅两套主题都要符合银豆豆风格

### 2.4 平台支持

- Windows 10 / Windows 11
- macOS 12 及以上版本
- 两个平台功能完全一致

---

## 第三部分：v1 之后的升级设想（暂不开发，仅记录想法）

- 每年重复的事件（生日、纪念日自动滚动到下一年）
- 更多小组件皮肤 / 主题
- 云同步（远期；如要做，必须先和用户讨论方案）

---

## 第四部分：技术栈决定记录（用户已选定，不再重复讨论）

以下方案已于 2026-09-16 由用户从候选方案中亲自选定：

| 决策项 | 选定方案 | 通俗解释 | 选择理由 |
|---|---|---|---|
| 软件框架 | **Electron** | 全球最成熟的桌面软件框架（微信电脑版、VS Code 同款），用网页技术做界面再套上桌面软件的壳 | 最成熟稳定、资料最多、出问题最容易解决；桌面悬浮小组件支持最完善 |
| 界面框架 | **Vue 3** | 国内最流行的界面开发框架 | 中文资料最丰富、写法简洁，适合快速做出符合国人审美的界面 |
| 数据存储 | **JSON 文件** | 数据保存在本地一个文本小文件里 | 结构简单、几乎不出毛病，备份就是复制文件，对本软件的数据量完全够用 |
| 开发语言 | **TypeScript** | JavaScript 的加强版，写代码时多写一些标注，工具就能自动查出低级错误 | 像"自动纠错笔"，错误在写代码阶段就被发现，软件更稳，适合长期维护 |
| 开发/构建工具 | **electron-vite** | 用"Vite"（以快出名的组装工具）来开发打包 Electron 软件 | 启动快、改一下界面立刻能看到效果，和 Vue 3 是黄金搭档，资料多 |
| 界面组件库 | **Element Plus** | 国内最流行的 Vue 现成零件库（饿了么团队出品），日期选择器、弹窗、开关等都是现成的 | 中文文档最全、零件最成熟、出问题最好找答案；外面会再包一层"银豆豆"皮肤，不用它的原样风格 |
| 包管理器 | **npm** | Node.js 自带的"装零件工具" | 不用额外安装任何东西，最稳妥；并配置了国内下载镜像（阿里 npmmirror）加快速度 |
| 图片存储 | **独立图片文件夹**（2026-09-16 选定） | 事件图片自动压缩后存到软件自己的 images 文件夹（用户数据目录下），事件数据里只记文件名 | 数据文件轻快、图片再多也不卡；将来备份导出时把文件夹一起打包即可 |
| 打包工具 | **electron-builder**（2026-09-16 选定） | 最流行的 Electron 打包工具，一条命令打出安装包 | 配置简单、自动处理图标/快捷方式/卸载、中文资料多、以后出 Mac 版也用它 |
| 安装包形式 | **NSIS 安装版**（2026-09-16 选定） | 双击"下一步下一步"安装，带快捷方式和卸载 | 最正式；开机自启功能需要安装版才生效 |
| 应用图标 | 白色圆盘 + 三颗红豆豆（用户选定，和界面 🫘 形象一致） | 由 scripts/gen-icon.js 纯代码生成 16/32/256 三尺寸 | 想改图标改脚本重跑即可 |
| 安卓版技术 | **Capacitor**（2026-09-16 选定） | 把现有 Vue 界面装进原生安卓外壳，变成真正的安卓 App | 界面代码复用九成，电脑版手机版一处改两处生效 |
| 安卓打包方式 | **本机打包**（2026-09-16 选定） | 在用户的 Windows 电脑上安装 Android 开发工具（JDK + Android SDK，约 3~4GB）直接出 APK | 自主可控，随时出包，不依赖外部服务 |

> 注意：以上为已定项。后续开发中如出现**新的**技术决策（如：具体 UI 组件库的选择、打包工具的选择等），仍必须按【第一部分】规则列方案交由用户决定。

---

## 第五部分：开发流程约定

- 开发按「功能一 → 功能六」逐步推进，每个功能完成先给用户看效果，确认后再进行下一个
- 每次开发会话开始前，先读取本文件，遵守其中的规则和需求
- 需求如有变化，先更新本文件，再动手改代码
- Git 版本管理：提交代码前告知用户；推送到远程仓库、创建远程仓库等对外操作必须先征得用户同意

### 开发环境备忘（2026-09-16 搭建骨架时踩过的坑，已解决，勿再踩）

- **启动软件的命令**：`env -u ELECTRON_RUN_AS_NODE npm run dev`
  （原因：从 VS Code / Claude Code 里启动时，环境里带着 `ELECTRON_RUN_AS_NODE=1` 干扰变量，会让 Electron 误以为是普通程序、不显示窗口，必须去掉）
- **重启软件的正确姿势**：先停掉 dev 任务，再执行 `taskkill //F //IM electron.exe` 清掉残留进程，最后重新启动。直接停任务不会杀死已弹出的软件窗口，会留下"僵尸进程"（表现为端口变成 5174、GPU 缓存报错、新旧界面混用）
- **改了"外壳/传话员"代码（src/main、src/preload）后，运行中的软件可能不会完整重载**，界面会出现 `Cannot read properties of undefined` 这类错误——按上一条重启软件即可，数据不会丢
- **TypeScript 固定用 5.x 稳定版**，不要升级到 7（配套检查工具 vue-tsc 还不兼容 7 的新结构）
- **npm 已配置国内镜像**（项目里 `.npmrc` 文件，阿里 npmmirror，下载快）
- **Electron 主程序漏下时**的手动补下命令：
  `cd node_modules/electron && ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/" node install.js`
- **打包安装包**：`npm run dist`（先构建再打包，产物在 release/ 文件夹，NSIS 组件走国内镜像）
- **注意**：vue / element-plus 已放在 devDependencies（构建时打进 out/），不要挪回 dependencies，否则安装包会无谓变大

### 双平台架构（2026-09-16 起）

- 同一套界面代码（src/renderer）跑在两个平台：
  - **电脑版（Electron）**：数据走 preload 的 window.api → 主进程存 JSON 文件
  - **手机/网页版**：`src/renderer/src/platform/` 里的"网页数据引擎"用浏览器 IndexedDB 实现了**一模一样的接口**
- 界面代码**只认 `platformApi`**（platform/index.ts 自动判断跑在哪），不要直接用 window.api
- 手机上隐藏的电脑专属功能：桌面小组件、窗口控制按钮、开机自启、托盘
- 网页版构建：`npm run build:web`（产物 dist-web/，vite.config.web.ts）
- 安卓版分四步：①双平台改造（已完成）②装 Android 工具 ③Capacitor 生成安卓工程 ④Gradle 打 APK
- 手机版暂缺（后续加）：到点提醒的系统通知（需 Capacitor 本地通知插件）、**安卓桌面小组件（用户明确要求，阶段三安卓工程建立后实现，需写少量 Kotlin + RemoteViews）**

### 安卓开发环境备忘（2026-09-16 搭建，全部在 D 盘，国内镜像）

- **JDK 21**：`D:\android-tools\jdk-21`（清华 Adoptium 镜像；系统里的 Java 8 没动）
- **Android SDK**：`D:\android-sdk`（腾讯镜像手动组装：platforms/android-36、build-tools/36.0.0、platform-tools、licenses 手写许可哈希）
- **安卓工程**：`android/` 文件夹（Capacitor 8 生成；compileSdk 36 / AGP 8.13 / Gradle 8.14.3 / minSdk 24）
- **出 APK 命令**：
  ```
  npm run build:web && npx cap sync android
  cd android && JAVA_HOME=/d/android-tools/jdk-21 ANDROID_HOME='D:\android-sdk' ./gradlew assembleDebug
  ```
  产物：`android/app/build/outputs/apk/debug/app-debug.apk`
- **加速源**（已配置好，勿删）：gradle-wrapper.properties 用腾讯镜像；android/build.gradle 里阿里云 maven 在 google() 之前
- **安卓图标**：`node scripts/gen-android-icons.js`（白盘红豆豆，覆盖 mipmap 各分辨率，并移除 anydpi-v26 改用 PNG）
- 桌面小组件（电脑版）：不置顶、固定桌面模式；被 Win+D 最小化会自动弹回

### GitHub 仓库（2026-09-16 上线）

- 仓库：https://github.com/yingyingdou82-creator/yindoudou-timer（公开仓库）
- **默认分支是 `main`**（早期推到过 master，已合并进 main 并删除 master；今后提交推送都用 main）
- **提交邮箱必须用 GitHub 匿名邮箱** `282536943+yingyingdou82-creator@users.noreply.github.com`（已配置到本仓库 git config；用户开启了"保护私人邮箱"，用真实邮箱会被 GitHub 拒绝推送 GH007）
- **README 截图**：一律用"演示数据目录 + 截图模式"生成（绝不沾用户真实数据和桌面背景）；相关临时脚本只留本地，不上传仓库
