# 银豆豆计时 — 项目说明书（CLAUDE.md）

> 本文件是 Claude Code 的项目说明书，每次对话都会自动加载。
> 所有参与本项目的 AI 助手必须严格遵守本文件的规则，尤其是【第一部分：项目协作规则】。

---

## 第一部分：项目协作规则（最高优先级，必须遵守）

1. **用户背景**：用户是不懂编程技术的非技术用户，无法提供具体的技术要求。

2. **一切技术决策必须先列方案，由用户挑选决定**：
   - 任何技术选型（框架、数据库、第三方库、工具、重大结构改动等），必须列出**至少 2 个候选方案**；
   - 用通俗易懂的中文解释每个方案：它是什么、优点、缺点；
   - 给出推荐方案及推荐理由；
   - **由用户挑选决定**。未经用户选择，不得擅自敲定任何技术方案。

3. **每个技术点都要详细解释**：向用户介绍或使用任何技术时，必须用大白话解释清楚：这个技术是什么、为什么用它、对用户有什么影响；避免专业术语，不得不用的术语必须先用一两句话解释明白。

4. **沟通语言**：全程使用中文交流。

5. **重大改动先征得同意**：涉及 Git 推送、安装软件、修改系统设置、对外发送内容等操作，必须先讲清方案、征得用户同意后再执行。

---

## 第二部分：产品需求文档（v1）

### 2.1 产品概述

- **产品名称**：银豆豆计时
- **产品定位**：一款简洁可爱的多端日期倒计时 / 正计时工具，帮用户记住生活中所有重要的日子（考试、生日、纪念日、项目截止日、还贷日、假期……）
- **运行平台**：Windows 10/11、Android 6.0+（macOS 工程已就绪待打包）
- **数据隐私**：所有数据只保存在用户自己的设备上，不上传云端
- **界面语言**：全中文界面

### 2.2 核心功能

#### 功能一：事件管理

- 创建、编辑、删除、置顶、搜索事件
- 事件字段：

| 字段 | 说明 | 例子 |
|---|---|---|
| 名称 | 这个日子的名字 | 距离春节 / 宝宝出生 |
| 日期 | 填一个日期即可：过去的自动按正计时，未来的自动按倒计时 | 2027-02-17 |
| 时间（可选） | 几点几分；当天按「小时/分钟」精确显示 | 14:30 |
| 截止日期（可选） | 和开始日期组成「时间段」；显示自动分三阶段切换 | 9月20日～10月15日 |
| 提前提醒（可选） | 到点前系统通知提醒；需填了时间才可用 | 提前 30 分钟 |
| 计数方式 | 自然日 / 工作日 / 按周 / 按年 | 见功能三 |
| 包含起始日 | 起算的第一天算不算进计时 | 是 / 否 |
| 颜色标签 | 8 种柔和配色 | 珊瑚红 / 天空蓝 |
| 小图标 | emoji 图标 | 🎂 📚 ✈️ |
| 背景图片（可选） | 作为卡片背景；支持按钮选择 / 拖拽文件 / Ctrl+V 粘贴 / 网页拖图，自动压缩 | 比赛海报 |
| 备注 | 补充说明（可选） | 记得买蛋糕 |
| 归档 | 完成的事件收进"归档"，主界面不显示，可找回 | 是 / 否 |
| 显示在桌面小组件 | 是否放到桌面小组件上 | 是 / 否 |

#### 功能二：计时方向自动判断

- 只填一个日期，方向自动判断：
  - 未来：倒计时——「距离考研还有 88 天」
  - 过去：正计时——「宝宝出生已经 100 天」
  - 今天：「就是今天！」
- 倒计时到期后，第二天起自动转为正计时
- 填了时间的事件，当天精确显示：「还有 2 小时 15 分钟」→「已经 40 分钟」
- **时间段事件**三阶段自动切换：距离开始还有 X 天 → 距离截止还有 X 天（截止当天「今天截止！」）→ 已结束 X 天
- 时间段事件不提供「包含起始日」开关；提前提醒按**开始时间**计算

#### 功能三：四种计数方式

- **自然日**（默认）：「还有 88 天」「已经 100 天」
- **工作日**：只数工作日
  - 默认周一至周五；可选「叠加法定节假日」——内置国家放假安排（法定假日不算、调休补班算）
  - 节假日数据内置 2025、2026 年（来源：国务院办公厅通知，`src/shared/holidays.ts`）；每年 11 月公布次年安排后补数据；无数据年份自动按周一至五
- **按周**：「还有 1 周 2 天」；整周只显示周数；不足一周显示天数
- **按年**：「还有 1 年 1 个月 3 周 4 天」；为 0 的单位自动省略；按日历计算

**通用选项「包含起始日」**（默认关）：打开后起算第一天也计入，整体多 1 天；四种方式通用。

#### 功能四：桌面小组件 ⭐ 核心特色

**电脑（悬浮面板）**：
- 「🖥 小组件」按钮弹出选择清单：勾选任意事件上桌面，即时生效
- 高度随条数自动伸缩（每条约 58 像素）；超屏幕 80% 后内部滚动
- 宽度三档（窄/中/宽）、透明度三档，有记忆；标题栏拖动，位置记忆
- 固定在桌面（不置顶）；点击条目呼出主窗口；数据与主窗口实时同步

**安卓（系统桌面小部件）**：
- 长按桌面 → 小组件 →「银豆豆小组件」
- 最多显示 5 条勾选事件（按离今天最近排序），天数自动更新（每 30 分钟刷新）
- 点击小部件打开 App

#### 功能五：主窗口

- 卡片列表：接近方形的圆角卡片自动多列排布；置顶优先、离今天最近靠前
- 搜索、深色/浅色主题、字体大小四档（小/标准/大/特大，三端同步）
- 自绘标题栏（拖动/双击最大化/– □ ✕ 按钮）
- 系统托盘常驻；关窗口=隐藏到后台，退出走托盘菜单；可选开机自启（安装版生效）
- 设置面板：主题、字体大小、开机自启、备份导出/导入

#### 功能六：数据安全与备份

- 电脑版存本地 JSON（用户数据目录），手机版存应用私有 IndexedDB
- 导出：全部事件 + 图片打包成一个 `.json` 备份文件（文件名带日期）
- 导入：可选「合并导入」（追加，推荐换设备）或「替换导入」（完全还原备份）
- 备份文件校验：非本软件备份拒收；坏数据自动跳过

#### 功能七：归档

- 卡片一键归档：主界面、搜索、小组件、提醒全部排除
- 主界面底部「📦 归档的事件（N）」入口，抽屉内可恢复 / 删除

#### 功能八：到点提醒（电脑版）

- 提前时长可选：5 / 10 / 15 / 30 分钟、1 / 12 小时、1 天
- 系统通知弹出，点击回到软件窗口
- 需软件处于运行状态（含托盘后台）；错过不补发
- 安卓版系统通知：待开发

### 2.3 界面风格

- 「银豆豆」视觉：圆润豆子造型、银白主色调、柔和低饱和配色
- 背景图片卡片：图片铺满 + 半透明暗色渐变 + 白色文字
- 深浅两套主题统一风格

---

## 第三部分：技术选型记录（已由用户选定）

| 决策项 | 选定方案 | 选择理由 |
|---|---|---|
| 软件框架 | Electron | 最成熟的桌面框架（VS Code 同款），悬浮小组件支持完善 |
| 界面框架 | Vue 3 + TypeScript | 中文资料最丰富，类型检查保证长期维护质量 |
| 构建工具 | electron-vite（电脑）/ Vite（网页安卓） | 启动快、热更新体验好 |
| 界面组件库 | Element Plus（外覆银豆豆主题） | 中文文档全、组件成熟 |
| 包管理器 | npm（国内镜像） | 无需额外安装 |
| 数据存储 | 电脑：本地 JSON；手机：IndexedDB | 简单可靠、纯本地 |
| 图片存储 | 独立图片文件夹（自动压缩） | 数据文件轻快 |
| Windows 打包 | electron-builder + NSIS 安装版 | 配置简单，开机自启需安装版 |
| 安卓方案 | Capacitor（复用 Vue 界面 + 原生小部件） | 界面代码复用九成 |
| 应用图标 | 白盘红豆豆（`scripts/gen-icon.js` 代码生成） | 与产品形象一致 |

> 后续新的技术决策仍须按【第一部分】规则列方案由用户选定。

---

## 第四部分：架构与开发指南

### 架构

- 同一套界面代码（`src/renderer`）双端运行：
  - 电脑版：preload 提供 `window.api` → 主进程读写本地文件
  - 手机/网页版：`src/renderer/src/platform/` 用 IndexedDB 实现同接口
- **界面代码统一使用 `platformApi`**（自动适配平台），不要直接调用 `window.api`
- 事件数据校验双端共用：`src/shared/sanitize.ts`
- 代码结构：

```
src/
├── main/        # Electron 主进程（窗口、托盘、存储、提醒）
├── preload/     # 安全传话层
├── renderer/    # 界面（主窗口 + 小组件页面 + 平台适配层）
└── shared/      # 双端共用（类型、校验、节假日）
android/         # 安卓工程（Capacitor + 原生桌面小部件）
scripts/         # 图标生成等工具
docs/            # 文档与截图
```

### 常用命令

```bash
npm install        # 安装依赖
npm run dev        # 电脑版开发模式
npm run typecheck  # 类型检查
npm run dist       # 打 Windows 安装包（release/）

npm run dev:web    # 网页版开发服务器（手机同 Wi-Fi 可访问）
npm run build:web  # 构建网页版（dist-web/）

# 安卓（需本机 JDK 21 与 Android SDK，详见本地 DEV-NOTES.local.md）
npx cap sync android
cd android && ./gradlew assembleDebug
```

### 开发流程约定

- 功能逐个开发，完成后先给用户验收，确认后再继续
- 需求有变：先更新本文件，再改代码
- Git：提交前告知用户；推送等对外操作先征得同意
- 本机开发环境的详细备忘（路径、镜像、注意事项）见 `DEV-NOTES.local.md`（仅本地，不上传仓库）
